version: 2

sources:
  - name: raw
    database: proje1-493409
    schema: GZrawData

    tables:
      - name: sales
        identifier: raw_gz_sales

      - name: product
        identifier: raw_gz_product

      - name: ship
        identifier: raw_gz_ship